services:
  postgres:
    image: 'debezium/postgres:14-alpine'
    volumes:
      - postgres-volume:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: always
    environment:
      - DB_HOST=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres_cdc
    networks:
      - local

  localstack:
    # Pinned, not `:latest` — newer LocalStack tags gate startup behind
    # a Pro license token (LOCALSTACK_AUTH_TOKEN) even for
    # community-tier services like SNS/SQS, and this container refuses
    # to start without one (exit code 55). Same fix already applied in
    # examples/spring-boot-postgres-sns/docker-compose.yml.
    image: localstack/localstack:3.8.1
    ports:
      - "127.0.0.1:4510-4559:4510-4559"  # external service port range
      - "127.0.0.1:4566:4566"            # LocalStack Edge Proxy
    environment:
      - DEBUG=${DEBUG- }
      - DATA_DIR=${DATA_DIR- }
      - LAMBDA_EXECUTOR=${LAMBDA_EXECUTOR- }
      - DOCKER_HOST=unix:///var/run/docker.sock
      - HOSTNAME=localstack
      - HOSTNAME_EXTERNAL=localstack
    volumes:
      - "${TMPDIR:-/tmp/localstack}:/var/lib/localstack"
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "./shell-scripts/localstack:/etc/localstack/init/ready.d"
    networks:
      - local

volumes:
  postgres-volume:

networks:
  local:
    driver: bridge
